# TXSA assignment dependencies
#
# Install everything with:
#     pip install -r requirements.txt
#
# Two extra downloads are NOT pip packages and must be run once afterwards:
#     python -m spacy download en_core_web_sm
#         (needed by Part_A/Q5_Alternative_Tokenization/Q5_Joshua.ipynb)
#     NLTK corpora download automatically on first run via the guards in
#     clean_review.py and Q1_Tokenization.ipynb, but they require internet access.
#         punkt, punkt_tab, stopwords, wordnet, averaged_perceptron_tagger

# ---- core data / modelling ----
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
joblib==1.5.3

# ---- NLP ----
nltk==3.9.4
spacy==3.8.14
textblob==0.20.0
transformers==5.14.1        # Part_A Q5 (Genemor): BERT WordPiece tokeniser

# ---- plotting (Part_B Q1 EDA charts) ----
matplotlib==3.10.1
seaborn==0.13.2
wordcloud==1.9.6

# ---- notebook execution ----
ipykernel==7.2.0
